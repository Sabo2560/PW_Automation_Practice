Possible pages:
- `tests/example.spec.ts` references `https://www.automationplayground.dev/`
- It verifies the homepage title and navigates the "Get started" link to an Installation heading

API routes:
- No local API routes are defined in this repository

Where defined:
- `tests/example.spec.ts`: Playwright E2E test steps and page interactions
- `playwright.config.ts`: Playwright test configuration only, not page or API definitions